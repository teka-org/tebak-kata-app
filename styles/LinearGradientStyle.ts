import { StyleSheet } from "react-native";

export const LinearGradientStyles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
});